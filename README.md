#Node Chrome headless images

A pretty simple image to have node + headless chrome running together.
Built to serve as foundation in Gitlab-ci builds

The version is kept in sync with the node underlying image